
.ShoppingCart
    width: calc(100% - 4vw);
    height: calc(100vh - 10.6vw);
    padding: 2vw;
    display: flex;
    flex-direction: column;
    justify-content: space-between;

.Products
    height: fit-content;
    display: flex;
    flex-wrap: wrap;
    gap: 1vw;
    max-height: calc( 100% - 7vw);
    overflow-y: scroll;